# specify include all rules common to others
include: "rules/common.smk"

## Target:
rule all:
    input:
        #the last output
        "results/multiqc/raw/multiqc_data"

## Rules to include:

include: "rules/[rule1]"
include: "rules/[rule2]"
include: "rules/[rule3]"
include: "rules/[rule4]"
